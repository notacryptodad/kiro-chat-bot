[project]
name = "kiro-chat-bot"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot>=21.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
